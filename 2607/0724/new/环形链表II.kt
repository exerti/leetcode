package new

/**
 * 环形链表 II (LeetCode 142)
 * 示例: head = [3,2,0,-4], pos = 1 → 返回索引为1的节点(值为2)
 *
 * 快慢指针 + 数学证明，思路引导。
 */

class DetectCycle {

    // =====================================================================
    // 解法：快慢指针 O(n) O(1)
    // =====================================================================
    /**
     * ▎第一步：判断是否有环
     *   快指针每次走2步，慢指针每次走1步
     *   如果有环，它们一定相遇（为什么？）
     *   （提示：快指针相对慢指针每次追1步，环内距离有限 → 必相遇）
     *
     * ▎第二步：找到环的入口 — 关键的数学
     *   设：
     *     a = 头到环入口的距离
     *     b = 环入口到相遇点的距离
     *     c = 相遇点绕回环入口的距离（即环长 - b）
     *
     *   慢指针走过的距离: a + b
     *   快指针走过的距离: a + b + n*(b+c)  （多绕了 n 圈）
     *
     *   快指针速度是慢指针的2倍，所以：
     *     a + b + n*(b+c) = 2*(a + b)
     *   → a + b + n*(b+c) = 2a + 2b
     *   → n*(b+c) = a + b
     *   → a = n*(b+c) - b = (n-1)*(b+c) + c
     *
     *   也就是说：
     *   从头走到环入口 (a) = 从相遇点走 c 步（可能多绕几圈）
     *
     * ▎第三步：算法实现
     *   1. 快慢指针相遇后
     *   2. 一个指针回到 head，另一个留在相遇点
     *   3. 两个指针每次各走1步
     *   4. 再次相遇的点就是环入口！
     *
     * ▎手写推演 [3 → 2 → 0 → -4 → 2(回环)]
     *   slow始终走1步：3 → 2 → 0 → -4 → 2 (相遇)
     *   fast始终走2步：3 → 0 → 2 → -4 → 0 → 2 (相遇)
     *   往回推 a,b,c?
     *   相遇后：ptr1=2, ptr2=head(3)，各自走1步 → 再次相遇在?
     *
     * ▎边界
     *   空链表？只有一个节点？无环？
     */
    fun detectCycle(head: ListNode?): ListNode? {
        TODO()
    }
}

class ListNode(var `val`: Int) {
    var next: ListNode? = null
}

// =====================================================================
// 关联题
// =====================================================================
/**
 * ▎环形链表 I (LeetCode 141)
 *   只判断有没有环，不需要找入口
 *   直接快慢指针相遇即可
 *
 * ▎快乐数 (LeetCode 202)
 *   本质上也是链表判环问题
 *   每个数字的下一个数字 = 各位平方和
 *
 * ▎寻找重复数 (LeetCode 287)
 *   数组元素在[1,n]范围内，nums[i] 作为索引形成隐式链表
 *   等价于环形链表II！
 */
