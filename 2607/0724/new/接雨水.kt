package new

/**
 * 接雨水 (LeetCode 42)
 * 示例: [0,1,0,2,1,0,1,3,2,1,2,1] → 6
 *
 * 三种解法思路，只给思考过程，代码自己填。
 */

class TrappingRainWater {

    // =====================================================================
    // 解法1：双指针 O(n) O(1) ⭐
    // =====================================================================
    /**
     * ▎直觉
     *   一个位置能接多少水？取决于它左右两边最高的柱子。
     *   水量 = min(左边最高, 右边最高) - 当前高度
     *
     * ▎暴力思路 → 优化
     *   暴力：对每个位置 i，向左扫一遍找 leftMax，向右扫一遍找 rightMax → O(n²)
     *   怎么优化到 O(n)？
     *   （提示：leftMax 和 rightMax 可以边走边维护，不需要每次都扫描）
     *
     * ▎双指针的核心洞察
     *   设 left=0, right=n-1，维护 leftMax 和 rightMax。
     *   如果 leftMax < rightMax → 左边是短板，left 位置的雨水量由 leftMax 决定
     *   如果 rightMax < leftMax → 右边是短板，right 位置的雨水量由 rightMax 决定
     *
     * ▎手写推演 [0,1,0,2,1,0,1,3]
     *   left=0(LM=0), right=7(RM=3) → LM<RM → left 处水量? → left++
     *   left=1(LM=1), right=7(RM=3) → LM<RM → left 处水量? → left++
     *   ...继续推演到 left==right
     *
     * ▎为什么这道题常考？
     *   局部信息（leftMax / rightMax 最小值）即可决定全局结果
     *   这是"木桶原理"的编程体现
     */
    fun trap(nums: IntArray): Int {
        TODO()
    }

    // =====================================================================
    // 解法2：单调栈 O(n) O(n)
    // =====================================================================
    /**
     * ▎换个角度
     *   不计算每个柱子上方有多少水，而是按"行"来算。
     *   遇到一个比栈顶高的柱子 → 栈顶柱子形成了一个"坑"
     *
     * ▎栈里存什么？
     *   存索引（因为需要计算宽度 = 当前索引 - 栈顶前一个索引 - 1）
     *
     * ▎什么时候出栈？
     *   当前高度 > 栈顶高度 → 栈顶是坑底，当前是右墙
     *   栈顶弹出一个后，新的栈顶就是左墙
     *   水量 = (min(左墙, 右墙) - 坑底) * 宽度
     *
     * ▎手写推演 [2,1,0,2]
     *   i=0: 栈空 → push 0
     *   i=1: 1<2 → push 1
     *   i=2: 0<1 → push 2
     *   i=3: 2>0 → pop 2(坑底=0), 左墙=?, 右墙=?, 水量=?
     *        2>1 → pop 1(坑底=1), 左墙=?, 右墙=?, 水量=?
     *        2==2 → push 3
     *   总水量?
     *
     * ▎单调栈的通用模式
     *   "找下一个更大/更小的元素" → 单调栈
     *   本题是"找两边第一个大于它的元素"
     */
    fun trapStack(nums: IntArray): Int {
        TODO()
    }

    // =====================================================================
    // 解法3：DP 预计算 O(n) O(n)
    // =====================================================================
    /**
     * ▎最直观的优化
     *   暴力 O(n²) 的瓶颈：每个位置都要向左向右扫描
     *   能不能把 leftMax[i] 和 rightMax[i] 提前算好？
     *
     * ▎预计算
     *   leftMax[i]  = max(leftMax[i-1], height[i])      ← 从左到右扫一次
     *   rightMax[i] = max(rightMax[i+1], height[i])     ← 从右到左扫一次
     *   water[i]    = min(leftMax[i], rightMax[i]) - height[i]
     *
     * ▎手写推演 [0,1,0,2,1,0,1,3,2,1,2,1]
     *   leftMax:  [?,?,?,?,?,?,?,?,?,?,?,?]
     *   rightMax: [?,?,?,?,?,?,?,?,?,?,?,?]
     *   water:    [?,?,?,?,?,?,?,?,?,?,?,?] → 总和?
     *
     * ▎这个解法 vs 双指针
     *   DP 更直观易懂，但需要 O(n) 额外空间
     *   双指针是 DP 的空间优化版本 —— 不需要存整个数组
     */
    fun trapDP(nums: IntArray): Int {
        TODO()
    }
}

// =====================================================================
// 解法对比 — 填表
// =====================================================================
/**
 * ┌──────────┬────────────┬───────────────────────────────────┐
 * │  解法    │  时间复杂度 │  适用场景 / 一句话评价             │
 * ├──────────┼────────────┼───────────────────────────────────┤
 * │  双指针  │            │                                   │
 * │  单调栈  │            │                                   │
 * │  DP      │            │                                   │
 * └──────────┴────────────┴───────────────────────────────────┘
 */

// =====================================================================
// 变体思考
// =====================================================================
/**
 * ▎变体1：柱状图中最大的矩形 (LeetCode 84)
 *   接雨水是找"坑"，最大矩形是找"凸起"
 *   思路有什么关联？（都用到单调栈，但方向相反）
 *
 * ▎变体2：容器盛最多水 (LeetCode 11)
 *   和本题的区别？哪道简单哪道难？
 *   （提示：11 不需要考虑中间障碍物，42 需要考虑）
 *
 * ▎变体3：二维接雨水 (LeetCode 407)
 *   从一维扩展到二维，思路怎么变化？
 */
